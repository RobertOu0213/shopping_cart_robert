<nav class="navbar">
  <div class="container-fluid d-flex">
    {{#if user}}
      {{#equal user.role "admin"}}
      <h1>
        <a class="navbar-brand" href="/products">好好吃後台</a>
      </h1>
      <div class="d-flex">
        <span class="navbar-user mr-3">{{user.email}}</span>
        <a href="/admin/logout" class="btn btn-custom">登出</a>
      </div>
      {{/equal}}

      {{#equal user.role "user"}}
        <h1>
          <a class="navbar-brand" href="/products">好好吃登入</a>
        </h1>
        <div class="d-flex">
          <span class="navbar-user mr-3">{{user.email}}</span>
          <a href="/order" class="icon mr-3">
            <i class="fas fa-list-alt fa-lg"></i>
          </a>
          <a href="/cart" class="icon mr-3">
            <i class="fas fa-shopping-cart fa-lg"></i>
          </a>
          <a href="/users/logout" class="btn btn-custom">登出</a>
        </div>
      {{/equal}}
      {{else}}
      <h1>
        <a class="navbar-brand" href="/products">好好吃</a>
      </h1>
      <div class="d-flex">
        <a href="/cart" class="icon mr-3">
          <i class="fas fa-shopping-cart fa-lg"></i>
        </a>
        <a href="/users/login" class="btn btn-custom">登入</a>
      </div>
    {{/if}}
  </div>
</nav>

